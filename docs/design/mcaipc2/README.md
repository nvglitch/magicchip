# MCAIPC2 生图资料包

已确认：H04-BQ＋AXB35-02。

1. 先读 `MCAIPC2-AI生图设计要求.md`。
2. 每次打开 `prompts` 的一份 TXT，上传开头指定的 `assets` 参考图，复制提示词。
3. 先做 01，确认后继续 02–09；完整参数表建议后期用可编辑文字排版。

包内包含 1 份完整设计要求、9 份独立提示词、6 张参考图。图片仅是产品/风格参考，本包尚未生成广告成图。
